#include "rfs.h"

// int _bitmap_find_last(Bitmap & bitmap)
// {
//     int counter = 0;
//     for (int i = 0; i < size_bitmap; i++)
//     {
//         for (int j = 0; j < 8; j++)
//         {
//             if ((bitmap.byte[i] & (0x80 >> j)) == 0) counter++;
//             else return (counter - 1);
//         }
//     }
//     return counter; // 32 full
// }

// int _bitmap_allocate(Bitmap & bitmap)
// {
//     if (auto last = _bitmap_find_last(bitmap); last == number_bits) // fulll
//     {
        
//     }

// }