#include "test.h"
#include "flash_bitmap.h"
#include "flash_driver_software.h"

TEST_CASE("test bitmap")
{
    // _init();

    // Flash_sector s(0, &driver);
    // s.reset();
    
    // Flash_bitmap b(0, s);

    // for (int i = 0; i < 20; i++)
    // {
    //     b.clear(i);

    //     REQUIRE(b.is_cleared(i).value == true);
    // }
    

    // REQUIRE(b.count_cleared().value == 20);
}